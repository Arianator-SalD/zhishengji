# 验证记录

2026-09-12，本机验证完成。

- Python 后端/服务商协议测试：40 passed。覆盖文本流、ASR/TTS 协议、错误、超时、音频限额、请求关联、打断、播音确认、会话隔离。存在两条依赖弃用警告，不影响测试结果。
- Node AudioWorklet 采样测试：3 passed，覆盖 48kHz、44.1kHz、16kHz 到 PCM16k 的连续帧相位。
- 浏览器真实页面：未填密钥时明确显示待配置；点击发送不输出伪造回答；四个参考稿可展开。
- 独立 `tests.browser_harness:app`（仅本地测试替身）：两轮文字输入、流式 PCM 播放完成；合成测试音源经过实际 AudioWorklet 与 WebSocket，停顿自动提交后收到测试转写和回答，音轨状态最终为 ended。
- 桌面与 390px 手机视口检查：咨询页无横向溢出，语音操作可见。
- 独立复审确认修复：关闭/取消后延迟操作继续执行、旧轮事件混入、新采样块相位损失、错误被成功状态覆盖。

尚未验证：真实账户鉴权、ASR 中文准确率、DeepSeek 回答质量、真实音色、真实网络下首音延迟和播放连续性。需要本地 `.env` 密钥后完成这部分。

原 HTML 仅被读取，实施在项目副本中。原文件 SHA-256：`14e66bfb891f347e21df033b4e0705c9347347b03d9ed986f4a0d1f6c66915f6`。
