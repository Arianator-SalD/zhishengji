# 新版 HTML 对比与 Sally 移植说明

本次以用户提供的 `职升机_Web交互Demo (4).html` 为界面基准，与首版原型和部署前的线上代码分别对比。新版原文件完整保存在 [prototype/职升机_Web交互Demo (4).html](<prototype/职升机_Web交互Demo (4).html>)；运行入口是 [static/index.html](static/index.html)。

## 新版改了哪里

| 模块 | 新版变化 | 移植后的处理 |
| --- | --- | --- |
| 专家详情 | 调整 AI 身份说明与介绍，移除“预约真人咨询”按钮，保留 AI Chat、推荐问题和底部语音入口 | Sally 使用相同结构，保留原头像、金融／AI 产品背景和四个精选问题 |
| 文字聊天 | 新头像与消息布局、时间标记、思考动画、回答卡片，以及“有帮助／复制／继续追问”操作 | Sally 的真实流式回答使用相同组件；复制取完整回答，不取初始空占位 |
| 模拟问答 | 从固定“三个部分”改为按问题关键词、专家视角和追问生成 Demo 回复 | 其他专家沿用新版规则，分别保存本页内的对话与草稿，不调用 Sally 后端 |
| 语音咨询 | 左侧专家资料与控制按钮，右侧实时对话；增加角色标签、转写行、头像和对话记录入口 | Sally 保留火山识别、DeepSeek、火山合成链路；中间按钮开始／手动发送，左侧停止回答，右侧结束。其他专家保留新版浏览器语音 Demo |
| 咨询记录 | 记录列表、Call／Chat 筛选、独立纪要详情、行动建议、原文回看和继续咨询 | 保留新版 Demo 数据与交互；查看原文不调用真实模型，回看与当前录音分开 |
| 社区悬赏 | 新增标题、背景、标签、悬赏档位、专家邀请和附件状态表单，发布后插入本地信息流 | 保留前端演示流程，未新增真实支付或消息发送服务 |

## 保留的已有能力

- Sally 的头像、专家资料、四个精选 QA 和本人资料配置。
- 后端接口、环境变量、访问保护、原网站地址及 GitHub → Render 自动部署。
- 按句生成播放、停止回答、手动结束录音和每轮 60 秒上限，没有恢复短暂停顿自动提交。
- 专家间消息隔离：切换时停止 Sally 的回答，迟到回复不进入其他专家对话。
- 用户头像，以及行动计划勾选、进度同步和浏览器本地保存。

Sally 当前从零开始，仅使用本次对话上下文，界面对应显示“基于本次对话”。新版中的长期职业画像、历史咨询记录和自动纪要仍是 Demo 展示，没有在此次迁移中变成真实后端服务。

## 后续修改入口

| 文件 | 用途 |
| --- | --- |
| `static/index.html` | 新版页面、专家资料、默认 Demo 消息、`generateExpertReply` 模拟回复规则、咨询记录与社区交互 |
| `static/consultation-ui.js` | 将新版聊天卡片、语音界面和按钮连接到 Sally 后端 |
| `static/demo-chat.js` | `demoChatConfig` 管理回复延迟和模拟规则入口；各专家会话隔离 |
| `static/voice.js` | 真实对话连接、录音生命周期、音频播放与打断 |
| `static/voice.css` | 后端相关控件及小屏适配；主体视觉沿用新版 HTML 的样式 |
| `content/` | Sally 本人资料和精选问答 |

继续改界面时，请以仓库运行版本为基础开分支。直接用另一份 HTML 覆盖 `static/index.html` 会丢失适配；更新原型时需保留适配脚本引用及页面元素 ID。没有改动本轮期间其他协作者更新的公开 README。

## 验证记录

- 9 位其他专家身份、按主题模拟回复、无模型请求、纯文本输入、会话隔离；Sally 文字路由通过本地模拟供应商验证。
- 模拟录音器发送静音 PCM 帧超过 4.5 秒，不提前结束；手动发送仅提交一次，转写不重复，回答通过语音链路播放。
- 新版复制、有帮助、继续追问、其他专家语音 Demo、咨询筛选／原文回看和本地悬赏发布通过验证。
- 行动计划切换、进度联动、刷新保存及损坏数据恢复通过浏览器回归。
- 实际 AudioWorklet 采样算法的 16 kHz、44.1 kHz、48 kHz 三项测试通过。
- 后端回归 52 项通过；新版原型留存文件与用户原文件逐字节一致。
- 测试浏览器原生 AudioWorklet 加载挂起，浏览器录音回归使用模拟录音器；这不等同于实机麦克风或真实火山接口测试。本轮没有读取或重新配置真实 Key。
